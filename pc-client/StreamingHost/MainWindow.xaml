<Window x:Class="StreamingHost.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Streaming Host" Height="520" Width="640"
        Background="#0a0a0a" Foreground="White" FontFamily="Segoe UI">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Streaming Host" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Signaling URL:" Width="110" VerticalAlignment="Center"/>
            <TextBox x:Name="SignalingUrlBox" Width="380" Text="ws://localhost:8080/ws"/>
            <TextBlock Text="Room:" Width="60" Margin="14,0,0,0" VerticalAlignment="Center"/>
            <TextBox x:Name="RoomCodeBox" Width="80"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="StartButton" Content="Start" Width="90" Height="30" Click="StartButton_Click"/>
            <Button x:Name="StopButton"  Content="Stop"  Width="90" Height="30" Margin="8,0,0,0" Click="StopButton_Click" IsEnabled="False"/>
            <TextBlock x:Name="StatusText" Text="Idle" Margin="20,6,0,0" Foreground="#aaa"/>
        </StackPanel>

        <ListBox Grid.Row="3" x:Name="LogList" Background="#111" Foreground="#ddd" BorderThickness="0" FontFamily="Consolas" FontSize="12"/>

        <TextBlock Grid.Row="4" Foreground="#777" Margin="0,8,0,0"
                   Text="Phase 1 — signaling round-trip only. Capture + encoder land in next pass."/>
    </Grid>
</Window>
